module.exports = class Collection extends Map {
    set(key, value) {
        super.set(key, value)
    }

    get(key) {
        super.get(key)
    }

    delete(key) {
        super.delete(key)
    }
}
